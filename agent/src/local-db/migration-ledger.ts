// Compatibility re-export only. Migration definitions, identity, ordering,
// validation and orchestration are owned by packages/persistence/migrations.
export * from "@naibaji/persistence/migrations";
