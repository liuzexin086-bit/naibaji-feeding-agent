# 奶爸机极简日调控模型：文字与公式说明

对应脚本：`E:\plan\feeding-model-simple.js`
模型版本：`naibaji-simple-v2`

## 一、模型目的

批次开始时只输入一次仔猪日龄。此后每天只输入：

1. 教槽料采食等级：无、低、中、高；
2. 腹泻等级：无、轻度、严重。

模型每天输出：

- 当天头均奶粉总量；
- 正常计划餐次；
- 腹泻调整后的实际餐次；
- 每餐头均奶粉量；
- 腹泻取消的餐次；
- 预计日增重和日末体重；
- 调控动作及风险提示。

所有奶粉量均为头均干奶粉克数，不是稀释后的奶液体积。

## 二、有效范围和固定参数

模型有效日龄：

$$
3 \le A_t \le 30
$$

其中：

- $A_t$：第 $t$ 个批次日的仔猪日龄；
- 批次首日 $t=0$；
- $A_t=A_0+t$。

固定参数：

| 参数 | 符号 | 数值 |
|---|---:|---:|
| 奶粉料肉比 | $FCR$ | 0.9 |
| 稀释总份数 | $D$ | 6 |
| 胃容量系数 | $V$ | 30ml/kg |
| 安全系数 | $S$ | 0.85 |
| 基准餐次 | $N_0$ | 12餐/天 |
| 最少餐次 | $N_{\min}$ | 2餐/天 |
| 教槽营养当量 | $E_c$ | 0.7 |
| 控奶增重保护线 | $G_{\min}$ | 180g/天 |

## 三、初始体重

3～21日龄使用体重标准表：

| 日龄 | kg | 日龄 | kg | 日龄 | kg |
|---:|---:|---:|---:|---:|---:|
| 3 | 2.3 | 10 | 3.6 | 17 | 5.0 |
| 4 | 2.4 | 11 | 3.8 | 18 | 5.2 |
| 5 | 2.6 | 12 | 4.0 | 19 | 5.4 |
| 6 | 2.8 | 13 | 4.2 | 20 | 5.6 |
| 7 | 3.0 | 14 | 4.4 | 21 | 5.8 |
| 8 | 3.2 | 15 | 4.6 |  |  |
| 9 | 3.4 | 16 | 4.8 |  |  |

22～30日龄按每天增加0.2kg外推：

$$
W_0=5.8+0.2(A_0-21)
$$

其中 $W_0$ 是批次首日估算体重，单位为kg。

## 四、胃容量与基线奶量

### 4.1 头均日安全奶粉容量

$$
C_t=\frac{W_t\times V\times S\times N_0}{D}
$$

代入固定参数：

$$
C_t=W_t\times30\times0.85\times12\div6
$$

所以：

$$
\boxed{C_t=51W_t}
$$

其中：

- $W_t$：当天起始估算体重，kg；
- $C_t$：头均日安全奶粉容量，g/头/天。

### 4.2 批次渐进比例

$$
R_t=
\begin{cases}
0.30,&t=0\\
0.60,&t=1\\
0.78,&t=2\\
0.85,&t\ge3
\end{cases}
$$

### 4.3 基线头均奶粉量

$$
\boxed{B_t=\operatorname{round}(C_tR_t)}
$$

其中 $B_t$ 是尚未经过餐次控制和腹泻减餐的整数基线奶粉量。

## 五、教槽料等级

教槽料等级同时决定：

1. 用于增重估算的代表采食量；
2. 批次首日的启动餐次；
3. 正常控奶的减餐间隔；
4. 等级高低排序。

| 等级 | 实际含义 | 代表值 $Q_t$ | 启动餐次 $S_g$ | 减餐间隔 $I_g$ | 等级序号 |
|---|---|---:|---:|---:|---:|
| 无 | 0g | 0g | 12 | 不减餐 | 0 |
| 低 | 大于0、低于30g | 15g | 11 | 每3天 | 1 |
| 中 | 30～低于70g | 50g | 10 | 每2天 | 2 |
| 高 | 不低于70g | 70g | 8 | 每1天 | 3 |

代表值只用于增重保护和体重递推，不代表现场真实称量值。

## 六、跨日正常餐次状态

模型把餐次分为两层：

- $N_t$：当天无腹泻时的正常控奶餐次，跨日保存；
- $F_t$：腹泻临时减餐后的当天实际餐次，只对当天有效。

### 6.1 批次首日

批次首日直接使用当天教槽等级的启动餐次：

$$
\boxed{N_0=S_g}
$$

即无、低、中、高分别从12、11、10、8餐启动。

### 6.2 教槽等级变差

设前一日教槽等级序号为 $L_{t-1}$，当日为 $L_t$。

如果：

$$
L_t<L_{t-1}
$$

表示采食等级变差，则：

$$
\boxed{N_t=\max(N_{t-1},S_g)}
$$

同时从当天重新计算减餐间隔。

### 6.3 等级相同或改善

如果：

$$
L_t\ge L_{t-1}
$$

则不恢复到启动餐次，保持现有正常餐次，再判断是否满足减餐条件。

因此，高档连续输入时不会出现：

```text
8 → 7 → 8 → 7
```

而是在增重保护允许时按以下方向递减：

```text
8 → 7 → 6 → 5 → 4……
```

## 七、正常减餐条件

只有无腹泻时才尝试正常减餐。

设：

- 当前正常餐次为 $N_t$；
- 目标餐次为 $N_t-1$；
- 距上次启动、恢复或成功减餐的天数为 $\Delta t$。

首先须满足：

$$
\Delta t\ge I_g
$$

并且：

$$
N_t>N_{\min}
$$

### 7.1 目标餐次下的候选奶量

每餐安全上限：

$$
P^{cap}_t=\frac{C_t}{12}
$$

减少1餐后的候选日奶量：

$$
M^{candidate}_t=
\min\left(B_t,\frac{C_t}{12}(N_t-1)\right)
$$

### 7.2 候选预计日增重

$$
G^{candidate}_t=
\frac{M^{candidate}_t+Q_tE_c}{FCR}
$$

即：

$$
\boxed{
G^{candidate}_t=
\frac{M^{candidate}_t+0.7Q_t}{0.9}
}
$$

若：

$$
G^{candidate}_t\ge180
$$

则允许：

$$
N_t\leftarrow N_t-1
$$

否则维持当前正常餐次。

## 八、腹泻临时减餐

腹泻当天不执行正常减餐判断，而是在当天正常餐次 $N_t$ 上临时取消餐次。

腹泻请求取消数：

$$
R_d=
\begin{cases}
0,&无腹泻\\
1,&轻度腹泻\\
3,&严重腹泻
\end{cases}
$$

为保证每天至少2餐，实际取消数为：

$$
\boxed{
R^{actual}_t=
\min(R_d,\max(N_t-2,0))
}
$$

当天实际餐次：

$$
\boxed{
F_t=N_t-R^{actual}_t
}
$$

腹泻取消餐次只影响当天，不写回跨日正常餐次 $N_t$。

### 8.1 严重腹泻的均匀取消位置

设实际取消 $r$ 餐，正常计划共 $N_t$ 餐，第 $j$ 个取消位置为：

$$
\boxed{
K_j=\operatorname{round}\left(\frac{jN_t}{r+1}\right),
\quad j=1,2,\ldots,r
}
$$

例如正常计划12餐、严重腹泻取消3餐：

$$
K_1=3,\quad K_2=6,\quad K_3=9
$$

即取消第3、6、9餐。

## 九、单餐量与实际日奶量

### 9.1 无腹泻时可用奶量

$$
M^{normal}_t=
\min\left(B_t,\frac{C_t}{12}N_t\right)
$$

### 9.2 单餐头均奶粉量

模型将单餐量向下保留两位小数：

$$
\boxed{
P_t=
\operatorname{floor}_{0.01}
\left(
\frac{M^{normal}_t}{N_t}
\right)
}
$$

使用向下取整，是为了避免舍入后超过每餐安全上限。

实现精度说明：脚本当前通过 JavaScript 浮点数执行
`Math.floor((value + Number.EPSILON) * 100) / 100`。由于二进制浮点表示误差，
个别数学上恰好已有两位小数的数仍可能被多减0.01g。例如：

```text
数学值：46 ÷ 10 = 4.60g
当前脚本实际输出：4.59g
```

因此，下文算例按当前脚本真实输出4.59g书写。该差异属于实现精度问题，不是业务公式要求。

### 9.3 当天实际头均奶粉量

$$
\boxed{M_t=P_tF_t}
$$

### 9.4 腹泻减少的头均奶粉量

$$
\boxed{\Delta M_t=P_tR^{actual}_t}
$$

因此，保留下来的每餐喂量不变，只通过取消完整餐次减少当天总奶量。

## 十、实际预计增重与体重递推

当天预计日增重：

$$
\boxed{
G_t=\frac{M_t+0.7Q_t}{0.9}
}
$$

单位为g/头/天。

下一日估算体重：

$$
\boxed{
W_{t+1}=W_t+\frac{G_t}{1000}
}
$$

下一日胃容量和基线奶量均使用新的估算体重重新计算。

## 十一、完整日调控顺序

每天按以下顺序执行：

1. 检查日龄是否超过30日龄；
2. 读取教槽料等级和腹泻等级；
3. 由当天估算体重计算安全容量 $C_t$；
4. 由批次日计算渐进比例 $R_t$；
5. 计算基线奶量 $B_t$；
6. 首日确定启动餐次，或根据教槽等级变化更新正常餐次；
7. 无腹泻时检查递减间隔和180g增重保护；
8. 得到当天正常餐次 $N_t$；
9. 根据腹泻等级临时取消0、1或3餐；
10. 得到实际餐次 $F_t$；
11. 计算单餐量 $P_t$、实际日奶量 $M_t$ 和减奶量 $\Delta M_t$；
12. 计算预计增重 $G_t$ 和日末体重 $W_{t+1}$；
13. 保存当天记录并进入下一日。

## 十二、7日龄算例

假设：

- 批次首日为7日龄；
- 初始估算体重 $W_0=3.0kg$；
- 教槽等级为“中”，代表采食量 $Q_0=50g$；
- 启动餐次为10餐。

### 12.1 安全容量

$$
C_0=51\times3.0=153g
$$

### 12.2 首日基线奶量

$$
B_0=\operatorname{round}(153\times0.30)=46g
$$

### 12.3 无腹泻

正常餐次：

$$
N_0=10
$$

单餐量：

$$
P_0=\operatorname{floorJS}_{0.01}(46\div10)=4.59g
$$

当天实际奶量：

$$
M_0=4.59\times10=45.90g
$$

预计增重：

$$
G_0=(45.90+50\times0.7)\div0.9\approx89.9g
$$

日末体重：

$$
W_1=3.0+89.9\div1000\approx3.09kg
$$

### 12.4 轻度腹泻

取消1餐：

$$
F_0=10-1=9
$$

单餐量仍为4.59g：

$$
M_0=4.59\times9=41.31g
$$

减奶量：

$$
\Delta M_0=4.59\times1=4.59g
$$

### 12.5 严重腹泻

取消3餐：

$$
F_0=10-3=7
$$

均匀取消位置：

$$
K=[3,5,8]
$$

单餐量仍为4.59g：

$$
M_0=4.59\times7=32.13g
$$

减奶量：

$$
\Delta M_0=4.59\times3=13.77g
$$

## 十三、输出字段含义

| 输出字段 | 含义 |
|---|---|
| `dayAge` | 当天仔猪日龄 |
| `batchDay` | 批次第几天 |
| `weightStartKg` | 当天起始估算体重 |
| `creepIntakeG` | 教槽等级对应的代表采食量 |
| `predictedGainG` | 当天预计增重 |
| `weightEndKg` | 当天结束估算体重 |
| `baselinePerPigMilkG` | 未经餐次控制的基线头均奶量 |
| `plannedFeedTimesWithoutDiarrhea` | 无腹泻时的正常餐次 |
| `requestedRemovedMeals` | 腹泻规则要求取消的餐数 |
| `removedMeals` | 受2餐下限保护后的实际取消数 |
| `skippedMealNumbers` | 被取消的餐序号 |
| `feedTimes` | 当天实际执行餐次 |
| `perFeedPerPigMilkG` | 每餐头均奶粉量 |
| `perPigMilkWithoutDiarrheaG` | 无腹泻时的正常日奶量 |
| `perPigMilkG` | 腹泻调整后的实际日奶量 |
| `milkReductionG` | 因腹泻取消餐次减少的奶粉量 |
| `action` | 当天调控原因 |
| `warnings` | 饲喂或健康风险提示 |

## 十四、状态保存和恢复

模型每天保存：

- 批次起始日龄；
- 当前批次日；
- 当前估算体重；
- 正常控奶餐次；
- 上一日教槽等级；
- 上次启动、恢复或成功减餐的日序号；
- 完整历史记录；
- 模型版本。

可通过：

```js
const snapshot = batch.getState()
const restored = restoreFeedingBatch(snapshot)
```

恢复批次。恢复时校验版本、日龄、体重、餐次、教槽等级和历史连续性。

## 十五、模型边界

- 本模型是群体头均饲喂控制模型，不是个体诊断或治疗模型。
- 教槽等级代表值是估算值，会影响预计增重和后续体重递推。
- 腹泻仅使用三级输入，无法表达腹泻头数、持续天数、脱水程度和病因。
- 严重腹泻提示应结合隔离、饮水检查和兽医判断执行。
- 若需要整批总奶粉量，须由上层系统再乘当天有效头数：

$$
M^{batch}_t=M_t\times H_t
$$

其中 $H_t$ 为当天有效仔猪头数。
