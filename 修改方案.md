# 控奶逻辑修改方案（已由 2026-08-03 持续教槽规则替代）

## 目标

当前有效规则：最近三次观察中至少两次达到同一档或更高才触发；餐次从 10 次开始，只减不增、每天最多减 1 次；低/中/高/极好最低为 9/8/6/4 次。下文旧方案仅作历史记录，不参与生产决策。

## 两个决策维度

| 维度 | 决定因素 | 作用 |
|:----|:--------|:----|
| **启动次数** | 教槽 3 日均值 | 从几次/天开始减 |
| **减少间隔** | 教槽 3 日均值 | 每几天减 1 次 |

## 启动次数（教槽阈值决定初始配奶数）

| 教槽 3 日均值 | 初始次数 | 含义 |
|:-------------:|:--------:|:----|
| ≥ 70g | **8 次/天** | 吃得好，快速减 |
| 30-70g | **10 次/天** | 一般，中等减 |
| < 30g | **11 次/天** | 没怎么吃，慢慢减 |

## 减少间隔（教槽决定递减速度）

| 教槽 3 日均值 | 减少间隔 | 含义 |
|:-------------:|:--------:|:----|
| ≥ 70g | **每 1 天**减 1 次 | 吃得好看减得快 |
| 30-70g | **每 2 天**减 1 次 | 一般，隔天减 |
| < 30g | **每 3 天**减 1 次 | 没吃好，慢减多保留 |

## 日增重保护（停止条件）

每次减少前检查：
```
单次量(g/头) = 计划头均奶粉(g/头) / 当前次数
if 单次量 > 胃容量 × 85%:
    实际可喝 = 胃容量 × 85% × 当前次数
else:
    实际可喝 = 计划头均奶粉

预测日增重(g) = 实际可喝 / FCR(0.9)
```

如果**预测日增重 < 180g** → **停止减少**，维持当前次数不再减。

## 示例

控奶从 D8 开启，教槽均值 50g → 启动 10 次，间隔 2 天：

```
D8:  10次 → 检查减到9次后增重？够180g → D10 减
D10:  9次 → 检查减到8次后增重？够 → D12 减
D12:  8次 → 检查减到7次后增重？够 → D14 减
D14:  7次 → 检查减到6次后增重？够 → D16 减
D16:  6次 → 检查减到5次后增重？不够180g → 维持6次
```

## 涉及改动

| 文件 | 改动 |
|:----|:-----|
| **feeding-model.js** `computeControlPlan` | 重写为渐进式递减逻辑，新增日增重检查 |
| **index.html** 表格/图表 | 配奶次数/单次量列跟随新逻辑 |
| **index.html** summary | 去掉「计划全期头均奶粉」字段 |
| **feeding-model.js** `applyBudget` | **移除整个函数**（不需要预算驱动了） |
