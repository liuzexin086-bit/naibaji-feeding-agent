# 改造方案 V2 — 减字段 + 增字段 + 简化工作流

> 2026-07-02

---

## 一、批次创建：减字段

### 1.1 初重改为选填（从体重表自动推算）

**动机**：用户的理想流程是"只输入日龄"。

**改动**（feeding-model.js 第 43-44 行）：
```js
// 改为：初重不填时从体重表推算，不报错
if (opts.startWeight == null || opts.startWeight <= 0) {
  opts.startWeight = WEIGHT_STANDARD[opts.startAge] || 2.3
}
```

UI 上初重字段保留，但标注「选填，不填则从标准表自动推算」。

### 1.2 加 `healthStatus`（批次级必填）

**模型层**：feeding-model.js 的 batch 导出中加入。

**UI层**：
- 批次弹窗中加 dropdown：`健康状况 normal / enteric-outbreak / respiratory-outbreak / mixed`
- 配置面板显示此字段

### 1.3 加 `farmRoom`（批次级必填）

**UI层**：
- 批次弹窗中加文本输入（如 `A-3`），默认值 `A-1`

---

## 二、每日录入：换字段 + 增字段

### 2.1 `sampleWeight` → `weighSample[]`（数组）

**动机**：全群称重应激大，改为大中小各 3-5 头。

**数据结构**（feeding-model.js）：
```js
weighSample: [
  { category: "large", avgKg: 3.5, headCount: 4 },
  { category: "medium", avgKg: 3.0, headCount: 8 },
  { category: "small", avgKg: 2.5, headCount: 3 },
]
```

**UI 层**（index.html）：
替换原来的单值 `sampleWeight` 输入框，改为：

```
📏 称重抽样（选填，大中小各称 3-5 头）
  大猪: 均重[___]kg × [__]头
  中猪: 均重[___]kg × [__]头
  小猪: 均重[___]kg × [__]头
```

**导出**（exportTrainingData）：
```json
"weighSamples": [
  {"category": "large", "avgKg": 3.5, "headCount": 4},
  ...
]
```

**权重推算**（新增函数 `computeWeightedAvg`）：
```js
function computeWeightedAvg(weighSample) {
  // 从抽样推算全群加权平均重
  const total = weighSample.reduce((s, w) => s + w.avgKg * w.headCount, 0)
  const count = weighSample.reduce((s, w) => s + w.headCount, 0)
  return count > 0 ? Math.round(total / count * 100) / 100 : null
}
```

### 2.2 加 `medication`（日级别选填）

**UI 层**：在死亡淘汰区下方或腹泻区下方加：

```
💊 用药（选填）
  [none / antibiotic / zinc-oxide / probiotic / oral-rehydration / other]
```

### 2.3 称重提醒（每周提醒）

在 `renderTodayCard()` 中，如果距离上次称重 ≥7 天，显示一个黄色 banner：

```
⚠️ 本周尚未称重，建议抽样称重大/中/小猪
```

不强制阻止推进，只提醒。

---

## 三、控奶模式：默认开启

**index.html 第 164 行**：checkbox 默认 checked。

但注意：首次加载时如果 `state.controlStartDay < 0`，应初始化为当前天。

```js
// init() 或 renderAll() 中
if (!document.getElementById('controlMode').checked && state.controlStartDay < 0) {
  document.getElementById('controlMode').checked = true
  state.controlStartDay = state.currentDayIndex
}
```

---

## 四、导出格式更新

### batch 元信息新增
```json
{
  "batch": {
    ...,
    "healthStatus": "normal",
    "farmRoom": "A-3"
  }
}
```

### dailyRecords 替换
```json
{
  "dailyRecords": [{
    ...,
    "weighSamples": [{"category":"large","avgKg":3.5,"headCount":4}, ...],
    "medication": "none"
  }]
}
```

### labels 保留不变
```json
{
  "labels": {
    "finalDiarrheaRate": ...,
    "totalMilkG": ...,
    "totalCreepG": ...,
    "avgPerPigMilkG": ...,
    "avgPerPigCreepG": ...,
    "pigDays": ...,
    "totalDiarrheaCases": ...
  }
}
```

---

## 五、改动汇总

| # | 文件 | 改动 | 行数 |
|:-:|:----|:----|:----:|
| 1 | feeding-model.js:43-44 | startWeight 改选填自动推定 | 2 |
| 2 | feeding-model.js:export | 加 healthStatus, farmRoom, weighSample, medication | 15 |
| 3 | feeding-model.js | 加 computeWeightedAvg 函数 | 8 |
| 4 | index.html:192-198 | 初重标注选填 + 加 healthStatus 下拉 + farmRoom 输入 | 10 |
| 5 | index.html:305-308 | sampleWeight → 大中小三组称重输入 | 20 |
| 6 | index.html:330-340 | 加 medication 下拉 | 8 |
| 7 | index.html:todayCard | 每周称重提醒 banner | 10 |
| 8 | index.html:checkbox | 控奶默认开启 | 3 |
| 9 | index.html:config面板 | 显示 healthStatus + farmRoom | 3 |
| 10 | index.html:advanceDay | 读取 weighSample + medication | 5 |
| 11 | index.html:export | 传 healthStatus + farmRoom | 3 |
| 12 | feeding-model.min.js | terser 重建 | — |

合计：约 87 行有效改动。
