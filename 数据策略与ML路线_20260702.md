# 奶爸机 — 数据策略与 ML 路线

> 2026-07-02

---

## 一、教槽料营养密度：0.7 系数验证

### 研究结论：系数合理，你的直觉正确

搜索了 大北农、正大、双胞胎、百宜 等主要品牌的公开营养数据：

| 来源 | 产品 | 消化能(kcal/kg) | 粗蛋白(%) | 赖氨酸(%) |
|:----|:----|:--------------:|:---------:|:---------:|
| 大北农 | 乳猪乐膨化料 | 3400 | 19-22 | ≥1.30 |
| 国标 GB/T 5915-2020 | 仔猪配合饲料 | — | 18-21 | 需符合 |
| 行业配方建议（高标） | — | ≥3600 | 20-21 | ≥1.5 |
| 专利无抗教槽料 | — | 3450(ME) | 20 | 1.4 |
| 行业常规 | 中档教槽料 | 3400-3500 | 19-20 | 1.3-1.5 |

全脂奶粉代谢能约 **4800-5200 kcal/kg**。

**计算**：3400÷4800 ≈ **0.71**，3600÷5200 ≈ **0.69** → **0.70 是合理的均值**

各品牌教槽料能量密度差异约 ±3%——确实差不多。但需注意：
- 0.7 只考虑了能量密度，实际生物价值更低（植物蛋白消化率 vs 乳蛋白）
- **保守建议改为 0.65**，等实测数据后再校准

---

## 二、腹泻病因太复杂 → 不诊断，只记录 confounder

### 原则
不要求饲养员判断「是什么导致的腹泻」，ML 不需要知道病因。
需要记录的是 **confounder（混杂因子）**，让模型能区分：
- "这个曲线导致腹泻" vs "这批猪恰好遇到了病毒"

### 建议新增字段

| 字段 | 级别 | 选填/必填 | 值 |
|:----|:----|:---------|:---|
| `healthStatus` | **批次级** | 必填 | `normal` / `enteric-outbreak` / `respiratory-outbreak` / `mixed` |
| `healthNote` | 批次级 | 选填 | 自由文本，如「确诊 PED」 |
| `medication` | **日级别** | 选填 | `none` / `antibiotic` / `zinc-oxide` / `probiotic` / `oral-rehydration` / `other` |
| `farmRoom` | 批次级 | 必填 | 如 `A-3` 表示 A 栋 3 栏 |

**为什么这么设计：**
- 不要求 PCR 检测，不增加饲养员负担
- `healthStatus` 让模型学到"这批有腹泻疫情"这个上下文
- `medication` 让模型学到"用药了所以腹泻降了"，避免错误归因
- `farmRoom` 捕获场区管理水平差异

### 不建议加的内容
- ❌ 具体病原体（PED、TGE、PCV2、PRRS）— 检测成本高、大部分场不做
- ❌ 细菌培养结果 — 时效性差、成本高
- ❌ 疫苗记录 — 全场统一，批次间无差异

---

## 三、ML 目标确认：增重反馈闭环

你的方向完全正确：

```
目标函数：最大化 ADG
约束条件：日腹泻率 ≤ 阈值（如 5%）
决策变量：每日奶粉量、配奶次数、粉水比
```

### 这意味着什么

**体重数据必须从选填改为必填。** 没有实测体重就无法计算 ADG，没有 ADG 就无法做 ML 训练。

最少称重频率（每个批次）：
| 时间点 | 用途 |
|:------|:-----|
| **D1**（进机） | 基线，替代估计初重 |
| **D7** | 中期增重评估 |
| **D14** | 第二阶段增重 |
| **终点**（D21-28） | 全期总增重 |

> 如果饲养员称重负担太重，最低方案：**D1 + 终点**，至少可以算全期 ADG。

### ML 训练流程（50 批后）

```
输入:
  - 批次参数: startAge, endAge, startWeight, headCount, healthStatus, farmRoom
  - 每日时序: dayAge, milkPerPig, creepPerPig, diarrheaRate, medication

标签:
  - 分段 ADG: D1-D7, D7-D14, D14-终点
  - 总 ADG

模型:
  - XGBoost (50-200 批): 预测 ADG，SHAP 分析特征重要性
  - LSTM/Transformer (200+ 批): 序列建模，最优曲线生成

输出:
  - 给定批次参数 → 预测最优每日奶粉量曲线
```

---

## 四、简化工作流：你的想法可拆为两阶段

你描述的理想流程：
> 饲养员只用输入当前日龄和结束日龄 → 自动形成配奶曲线 → 每天反馈教槽和腹泻 → 系统实时调参

### 第一阶段（现在，数据采集期）

当前 `E:\plan` 的流程已接近这个目标。要改的：

| 改动 | 原因 |
|:----|:-----|
| 初重改为选填（从体重表自动推算） | 你的目标之一是简化输入 |
| 每周称重改为**必填**提醒 | ML 需要增重数据 |
| 控奶模式默认开启 | 饲养员不需要手动开关 |
| 曲线自动调整控奶节奏（基于教槽反馈） | 当前已实现 `computeControlPlan` |
| 加 `healthStatus` 和 `medication` 字段 | 消除 confounder |

### 第二阶段（ML 训练后，闭环期）

```
批次创建: 输入 3d + 21d → 系统从体重表取初重 2.3kg
  ↓
系统生成曲线（胃容量 × 渐进比例，控奶默认开启）
  ↓
每日: 饲养员录入总配奶 + 总教槽 + 腹泻L/M/H
    → 系统调整次日推荐量（基于教槽差额 + 日增重保护）
    → 每周必填: 称重
  ↓
批次完成 → 自动导出训练数据
  ↓
(50 批后) ML 模型训练 → 校准系数:
  - FCR（当前 0.9 可能偏高或偏低）
  - 渐进比例（D1-D4+ 是否最优）
  - 控奶斜率表（W 依赖是否准确）
  - 教槽营养当量（0.7 还是 0.65）
  ↓
(200 批后) 闭环:
  - 系统直接输出最优曲线，不需人工校验
  - 异常检测: 当模型预测 vs 实际 ADG 偏差大时告警
```

### 当前代码需要改的

1. **加 healthStatus 到批次创建弹窗**（枚举下拉）
2. **加 medication 到每日录入界面**（选填，多选或下拉）
3. **体重从选填改为：每周提醒必填**（UI 上划线标注）
4. **控奶默认开启**
5. **初重可从体重表自动推算**（如果用户不填）

---

## 五、争议点记录

| 问题 | 当前选择 | 理由 |
|:----|:--------|:-----|
| 教槽营养当量 | 0.7（暂保留） | 文献支持，等数据校准 |
| 腹泻病因记录 | 只记录 confounder，不诊断 | 避免增加饲养员负担 |
| ML 算法 | 先用 XGBoost | 50 批不够深度学习 |
| 称重频率 | D1 + 每周 | 平衡数据质量和负担 |
| 场区效应 | 用 `farmRoom` 捕获 | 混合模型中的随机效应 |
