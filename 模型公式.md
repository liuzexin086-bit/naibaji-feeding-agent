# 奶爸机 — 核心模型公式

---

## 一、基线计划生成（generatePlan）

### 1.1 胃容量

```
容量(g/天) = 体重(kg) × 30(ml/kg) × 0.85(安全系数) × 12(次/天) ÷ 6(兑水1:5)
```

简写：`capacity = weight × 30 × 0.85 × 12 / 6`

### 1.2 渐进比例（适应期→平台期）

| 进栏天数 | 比例 | 含义 |
|:--------:|:----:|:----|
| D1（刚断奶） | 30% | 过渡适应，少量 |
| D2 | 60% | 快速上量 |
| D3 | 78% | 接近平台 |
| D4+ | 85% | 平台维持 |

### 1.3 每日奶粉量

```
头均奶粉(g/头) = 胃容量 × 渐进比例
总奶粉(g) = 头均奶粉 × 头数
```

### 1.4 体重增长

```
增重(kg) = 奶粉量(g) / (FCR × 1000)

FCR = 0.9（内部固定，不可调，用于数据采集后训练深度学习模型）
```

> 注意：体重仅作为计算胃容量的中间变量，不是最终输出。最终输出是奶粉量曲线。

---

## 二、断奶体重标准（参考线）

猪只各日龄的最低合格体重，仅作为参考展示，**不参与计算**：

```
D3≥2.3  D4≥2.4  D5≥2.6  D6≥2.8  D7≥3.0
D8≥3.2  D9≥3.4  D10≥3.6 D11≥3.8 D12≥4.0
D13≥4.2 D14≥4.4
```

---

## 三、控奶计划（computeControlPlan）

控奶的核心逻辑：**持续教槽触发，餐次从 10 次开始只减不增，每天最多减少 1 次。**

### 3.1 持续状态与餐次下限

```
最近三次观察中至少两次达到同一档或更高 → 次日开始控奶
低档（10）→ 最低 9 次/天
中档（45）→ 最低 8 次/天
高档（80）→ 最低 6 次/天
极好（130）→ 最低 4 次/天
```

### 3.2 递减过程

```
从 10 次开始，每个计划日最多减少 1 次：
  if 当前次数 ≤ 持续教槽档位下限 → 停止减少
  else 当前次数 = 当前次数 - 1

餐次只减不增，任何计划日不得超过 10 次。
```

> 注：教槽档位必须使用固定的无/低/中/高/极好记录，内部值为 0/10/45/80/130。

### 3.3 控奶后计划公式

```
单次上限(g/头) = 日安全容量(g/天) / 12

控奶后头均奶粉(g/头) = min(原计划, 单次上限 × 当前次数)
控奶后总奶粉(g) = 控奶后头均奶粉 × 头数

单次配奶量(g/批) = 控奶后总奶粉 ÷ 当前次数
```

### 3.4 在曲线上的显示

| 控奶状态 | 计划曲线 | 参考线 |
|:--------:|:--------|:------|
| 关闭 | 🟢 计划（原胃容量曲线） | — |
| 开启 | 🟢 **计划(控奶)**（递减后的曲线） | ⚫ 参考(不控奶) |

---

## 四、每日数据录入

### 4.1 每日录入字段

| 字段 | 单位 | 说明 |
|:----|:---:|:-----|
| 总配奶量 | g | 整批当天实际奶粉用量 |
| 总教槽量 | g | 整批当天实际教槽采食（可选） |
| 当前头数 | 头 | 推进时可减少 |
| 腹泻轻度 | 头 | 当天轻写腹泻头数 |
| 腹泻中度 | 头 | 当天中度腹泻头数 |
| 腹泻重度 | 头 | 当天重度腹泻头数 |
| 样本称重 | kg/头 | 可选抽样称重 |

### 4.2 衍生计算

```
头均奶粉(g/头) = 总配奶量 / 当前头数
头均教槽(g/头) = 总教槽量 / 当前头数
腹泻率(%) = (轻+中+重) / 当前头数 × 100
```

---

## 五、批次完成 — 训练数据导出

### 5.1 最终腹泻率标签

```
总腹泻头次数 = Σ(每天轻度+中度+重度腹泻头数)
总猪·天数 = Σ(每天头数)

最终腹泻率(%) = 总腹泻头次数 / 总猪·天数 × 100
```

该值作为深度学习模型的**标签**。

### 5.2 导出数据特征

每条训练样本包含：
```
dayAge, totalMilkG, totalCreepG, perPigMilkG, perPigCreepG,
headCount, diarrheaMild, diarrheaModerate, diarrheaSevere,
dailyDiarrheaRate, sampleWeight
```

标签：`finalDiarrheaRate`

---

## 六、参数来源汇总

| 参数 | 值 | 来源 | 说明 |
|:----|:---|:-----|:-----|
| FCR | 0.9 | 用户确认 | 内部固定，训练后优化 |
| 胃容量系数 | 30ml/kg | 文献/经验 | 每kg体重的胃容量 |
| 安全系数 | 0.85 | 经验 | 胃容量安全裕度 |
| 兑水比 | 1:5 | 标准 | 1g粉+5ml水 |
| 配奶次数 | 12次/天 | 设计 | 基线次数 |
| 渐进比例 | 30%/60%/78%/85% | 设计 | 过渡到平台 |
| 日增重下限 | 180g | 用户 | 控奶保护阈值 |
| 控奶下限 | 2次/天 | 用户 | 最少配奶次数 |
| 教槽折算 | 0.7 | 设计 | 教槽营养密度系数 |
