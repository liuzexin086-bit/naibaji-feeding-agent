# 奶爸机 — 超早期断奶饲喂数据采集系统

## 需求文档 v1.0

> 2026-06-30

---

## 1. 项目定位

面向 3-14 日龄超早期断奶仔猪的智能配奶数据采集系统。核心目标是采集真实饲喂数据，用于后期的深度学习模型训练，优化 FCR 和腹泻预测模型。

## 2. 核心流程

```
批次设定（日龄+初重+头数）
    → 自动计算饲喂计划（胃容量→头均奶粉→总配奶）
    → 每日录入实际饲喂数据（总配奶、总教槽、腹泻细化）
    → 推进到下一天 → 系统自动更新预测
    → 批次完成 → 自动导出训练数据文件（含最终腹泻率标签）
    → 批次进入只读状态，可供查看对比
```

## 3. 功能需求

### 3.1 批次管理

- **新建批次**：点击"新建批次"按钮，清空表单进入新批次设定
- **批次切换**：顶部下拉菜单列出所有批次，切换时保存当前批次，加载目标批次
- **自动恢复**：页面加载时自动恢复最近一次未完成的活跃批次
- **状态标识**：批次分为"活跃"和"已完成"两种状态，下拉菜单中区分显示

### 3.2 批次设定

- 字段：批次名称、超早期断奶日龄、最终断奶日龄、初重（kg/头）、断奶头数
- 断奶头数：在每日录入中可更新（如死亡淘汰导致头数减少）
- 输入验证：
  - 日龄范围 1-30 天
  - 超早期断奶日龄 < 最终断奶日龄（否则报错提示）
  - 初重 > 0
  - 断奶头数 > 0
- 批次参数一旦录入第一天的数据后，在批次完成前"批次设定"面板转为只读（不允许修改，防止数据丢失）
- 新建空批次时参数可自由修改

### 3.3 饲喂计划自动生成（后端模型）

- 使用胃容量公式计算每日头均奶粉量：
  ```
  胃容量(g粉/天) = 体重(kg) × 30ml/kg × 0.85 × 12次/天 ÷ 6(兑水)
  头均奶粉量 = 胃容量 × 渐进比例
  总配奶量 = 头均奶粉量 × 当前头数
  ```
- 渐进比例：D1=30%, D2=60%, D3=78%, D4+=85%
- FCR = 0.9（内部固定，用户不可调）
- 预计断奶体重 = 初重 + 全期奶粉/(FCR×1000)（仅奶粉，不含教槽）
- 体重标准表（3d≥2.3 ... 14d≥4.4）作为参考展示
- 自动评估断奶体重是否达标

### 3.4 每日数据录入

每日推进工作流：
1. 显示当天的计划数据（头均奶粉量、总配奶量、配奶次数）
2. 用户录入：
   - 总配奶量（g，整群当天实际用量）
   - 总教槽量（g，可选）
   - 当前头数（随批次推进可减少）
   - 腹泻轻/中/重各多少头
   - 样本称重（kg/头，可选）
3. 点击"录入并推进一天"：
   - 验证数据完整性（总配奶量必须 ≥ 0）
   - 保存当前天记录
   - 自动计算头均采食量、头均教槽量、腹泻率
   - 推进日龄
   - 更新图表
4. 最后一天（最终断奶日）：
   - 显示"断奶日"提示
   - 用户录入当天数据（配奶量为 0）
   - 点击"完成批次"按钮完成整个批次

### 3.5 数据展示

- **曲线图 1：头均配奶量（g/头）** — 柱状图，计划 vs 实际
- **曲线图 2：总配奶量（g）** — 柱状图，计划 vs 实际
- **每日明细表**：显示所有天的完整数据
- **统计摘要**：饲喂天数、计划全期奶粉、预计断奶重、达标情况、已录总奶粉、已录头均日奶粉

### 3.6 批次完成

- 最后一天录入后点击"完成批次"按钮
- 自动导出训练数据文件（JSON，浏览器下载）
- 批次状态切换为"已完成"
- 所有录入字段转为只读显示（不可修改、不可删除、不可继续推进）
- 数据持久化到 localStorage

### 3.7 训练数据导出

批次完成时自动导出：

```json
{
  "batch": { "startAge":3, "endAge":14, "headCount":100, ... },
  "dailyRecords": [
    { "dayAge":3, "totalMilkG":3500, "totalCreepG":0,
      "perPigMilkG":35.0, "diarrheaMild":0, "diarrheaModerate":0,
      "diarrheaSevere":0, "dailyDiarrheaRate":0.0, "sampleWeight":2.30 }
  ],
  "labels": { "finalDiarrheaRate": 2.5 }
}
```

`finalDiarrheaRate` 作为训练标签（总腹泻头次数/总 pig-days × 100%）。

### 3.8 数据持久化

- 使用浏览器 localStorage 存储所有批次数据
- 数据模型：
  - `naibaji_batches`: 批次对象字典（id → { config, plan, records, status, ... }）
  - `naibaji_current`: 当前批次 ID
- 后续 Electron 阶段迁移到文件系统

## 4. 已知限制 & 后续规划

- V1 为纯浏览器端，无后端 API
- V2 将使用 Electron 套壳，数据存本地文件
- V3 可考虑将数据用于深度学习模型训练

## 5. 技术栈

- 前端：纯 HTML + CSS + JS（无框架）
- 图表：Chart.js（CDN）
- 持续化：localStorage
- 运行方式：浏览器直接打开 index.html
